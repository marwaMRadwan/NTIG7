{{>header}}
{{>navbar}}
<section class="container my-5">
    {{#if err}}
    <div class="alert alert-danger" >
        there is an error
    </div>
{{else}}
<div class="row">
    {{#each tasks}}
    <div class="col-4">
        <div class="m-2 border border-3">
            {{this.id}}
        </div>
    </div>
    {{/each}}
</div>
{{/if}}
</section>
{{>footer}}